package main;

type BiggestFloat float64;
type Celsius BiggestFloat;
type Counter int;

func main() {
	var temp Celsius = 100.0;
	var count Counter = 0;
	count = 42;
	println("ok");
	print(temp);
};

/*

Se agregó un buffer por archivo 
que permita mantener cambios sin 
guardar al cambiar de archivo sin perderlos

*/